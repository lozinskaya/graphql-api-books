type Publisher {
  id: Int!
  title: String!
  books: [Book]
  authors: [Author]
}

input createPublisherInput {
  title: String!
}

type Mutation {
  createPublisher(createPublisherInput: createPublisherInput!): Publisher
}
